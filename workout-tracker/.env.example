# Rokid Workout Tracker — environment template
# Copy to .env and fill in values. Never commit .env.

# Firecrawl MCP (optional — for web search/scrape features)
FIRECRAWL_API_KEY=

# Exa MCP (optional — for neural search)
EXA_API_KEY=

# Rokid device connection (optional)
ROKID_DEVICE_IP=
ROKID_DEVICE_PORT=8080
