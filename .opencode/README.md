# .opencode/

Project-level OpenCode agent and command definitions.

- `agents/` — Subagent YAML definitions (builder, investigator, reviewer)
- `commands/` — Slash command definitions (build, test, lint, install)
